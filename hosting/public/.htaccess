# TS Palletium — Apache config (WebSupport hosting)

RewriteEngine On

# allow Let's Encrypt HTTP validation to pass through (no HTTPS/redirect)
RewriteRule ^\.well-known/acme-challenge/ - [L]

# force HTTPS
RewriteCond %{HTTPS} !=on
RewriteCond %{HTTP_HOST} !^localhost [NC]
RewriteRule ^ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# force bare domain (www.tspalletium.com -> tspalletium.com)
RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
RewriteRule ^ https://%1%{REQUEST_URI} [L,R=301]

# protect internal folders and library files
RewriteRule ^(data|seed|templates)(/|$) - [F,L]
RewriteRule ^(lib|config)\.php$ - [F,L]

# route page URLs through the PHP router (real files are served directly)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteRule ^ index.php [L]

AddDefaultCharset UTF-8

<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css text/plain text/xml application/javascript application/json image/svg+xml
</IfModule>

<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpeg "access plus 30 days"
  ExpiresByType image/png "access plus 30 days"
  ExpiresByType text/css "access plus 7 days"
  ExpiresByType application/javascript "access plus 7 days"
  ExpiresByType text/html "access plus 0 seconds"
</IfModule>

<IfModule mod_headers.c>
  Header set X-Content-Type-Options "nosniff"
  Header set X-Frame-Options "SAMEORIGIN"
  Header set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>
